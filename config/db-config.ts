const config = {
    PSQL_DB: {
        client: "pg",
        connection: {
            host: "localhost",
            port: 5432,
            user: "postgres",
            password: "root",
            database: "Amora",
        },
    }
}

export default config;